#!/bin/sh
if [ $1 == exclude ]; then
	gitdir="`git rev-parse --absolute-git-dir`/info/exclude"
	echo '\n' >> $gitdir/info/exclude
	git status -s | grep \?\? |  awk '{ print $2 }' >> $gitdir/info/exclude
elif [ $1 == rm ]; then
	rm -r `git status -s | grep \?\? |  awk '{ print $2 }'`
fi
